import RegistrationPage from '@/components/pages/RegistrationPage';
import React from 'react';

const page = () => {
    return (
        <div>
            <RegistrationPage/>
        </div>
    );
};

export default page;